defaults:
  - experiment: fed_avg
  - _self_

device: auto

partition_strategy: "iid"

dataset:
  name: mnist_fashionmnist
  # name: cifar10_fashionmnist
  data_path: ./data
  normalize: true

hydra:
  run:
    dir: .
  output_subdir: null
